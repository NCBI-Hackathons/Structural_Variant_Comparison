import os, sys, glob

name = 'pydbVar'
version = '0.1'

from distutils.core import setup
from distutils.extension import Extension


metadata = {
        'name': name,
        'version': version,
        'cmdclass': cmdclass,
        }


if __name__ == '__main__':
    pass
